@model List<Resource>

<h2>Resources</h2>

<div class="card-container">
    @foreach (var item in Model)
    {
        <div class="card">
            <h3>@item.Name</h3>
            <p>Quantity: @item.Quantity</p>
        </div>
    }
</div>